html, body {
	background-color: #000;
	min-height: 100vh;
}

.card {
	background: rgba(255, 255, 255, .4);

	.row {
		margin-top: 0.5em;
		margin-bottom: 0.5em;
	}
}

.photo {
	border-radius: .25em;
}

.primary-links {
	.row {
		margin-top: 0;
	}

	.btn {
		margin: 0.5em 0 0 0.5em;
	}
}
